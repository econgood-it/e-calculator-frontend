# Run locally


Create an .env file with the following content:

```shell
VITE_BACKEND_DOMAIN=https://balance-sheet-api.dev.econgood.org
VITE_FRONTEND_DOMAIN=http://localhost:3000
```

Then run the following commands:


```shell
$ yarn install
$ yarn start
```


